---
import { siteInfo } from "../data/siteMetadata.ts";
import navigation from "../data/navigation.ts";
---

<nav class="mainnav row">
  <a href="/" class="siteTitle">I am {siteInfo.title}.</a>
  <ul>
    {
      navigation.map((i) => (
        <li>
          <a href={i.href}>{i.title}</a>
        </li>
      ))
    }
  </ul>
</nav>
